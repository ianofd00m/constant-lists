# Production Environment Variables
# Copy this to your Render dashboard environment variables

NODE_ENV=production
PORT=3001

# MongoDB Atlas Connection (you'll get this from MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/constant-lists?retryWrites=true&w=majority

# JWT Secret (generate a strong random secret)
JWT_SECRET=your-super-secure-jwt-secret-here-make-it-long-and-random

# Email Configuration (optional for now)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# CORS Configuration
CORS_ORIGIN=https://constant-lists.netlify.app